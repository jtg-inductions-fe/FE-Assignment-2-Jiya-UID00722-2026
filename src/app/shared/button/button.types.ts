export enum ButtonVariant {
  basic = 'basic',
  raised = 'raised',
  flat = 'flat',
  stroked = 'stroked',
  icon = 'icon',
  fab = 'fab',
  'mini-fab' = 'mini-fab',
}
export enum ButtonColor {
  primary = 'primary',
  accent = 'accent',
  warn = 'warn',
}
export enum ButtonType {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}
export enum IconPosition {
  prefix = 'prefix',
  suffix = 'suffix',
}
